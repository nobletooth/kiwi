version: v2
clean: false # Allows *.proto files to remain in the output directory if they are not regenerated.
plugins:
  - local: protoc-gen-go
    out: proto
    opt: paths=source_relative
    include_imports: true
inputs:
  - directory: proto
